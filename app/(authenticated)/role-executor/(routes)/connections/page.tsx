import { redirect } from 'next/navigation';

export default function ExecutorConnectionsPage() {
  // For now, redirect to the main executor page
  // This will be replaced with the actual connections page implementation
  redirect('/role-executor');
} 